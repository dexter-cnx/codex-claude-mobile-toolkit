# Flutter Build Benchmark Strategy

## Minimum benchmark surfaces

- `flutter pub get`
- `flutter analyze`
- Android debug build
- Android release build
- web build if enabled
- iOS build if host allows
- artifact size where relevant
