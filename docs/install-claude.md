# Install for Claude

Keep this repository accessible to the project context so Claude Code can read `AGENTS.md`, `CLAUDE.md`, and the `skills/` directory. Start from a prompt file when you want consistent output shape.
