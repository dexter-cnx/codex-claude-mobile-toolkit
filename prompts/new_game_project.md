Read `AGENTS.md` and `CLAUDE.md` first.

Create a Flutter + Flame + Riverpod + Clean Architecture game scaffold for a deterministic turn-based grid puzzle.

Requirements:
- pure Dart domain rules
- Flame renderer/input surface
- Riverpod orchestration
- JSON-authored level support
- testable turn resolver
- room for telemetry hooks and solver pipeline later
