# prompts/apply_approved_build_fixes.md

Read `AGENTS.md` first.

## Goal

Apply only approved Flutter build optimization actions and verify whether the approved changes improved the benchmark.

## Required inputs

Read:

- `.build-benchmark/optimization-plan.json`
- `.build-benchmark/approved-actions.json`
