# Prompt: Reformat rules for Claude Code

Convert the attached `flutter_master_rules.md` into a production-ready `CLAUDE.md` for a Flutter/Dart repository.

Requirements:
- Preserve architectural intent and quality bar.
- Keep instructions concise but strong.
- Assume Claude Code has no hard file-size limit, but avoid unnecessary verbosity.
- Optimize for implementation planning and safe code edits.
- Include a structured response contract:
  1. assumptions
  2. proposed file changes
  3. implementation details
  4. risks
  5. tests
- Output only the final `CLAUDE.md` content.
