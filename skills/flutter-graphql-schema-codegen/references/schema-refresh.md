# Schema Refresh

Schema refresh should be explicit and reviewable.

Prefer a script or documented command that states:

- endpoint source,
- required environment variables,
- auth/header expectations,
- output path.

Treat schema updates as visible code-review events.
