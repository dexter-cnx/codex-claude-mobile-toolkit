# UI Consistency

Look for:
- spacing drift
- multiple versions of the same button/input/card pattern
- inconsistent title hierarchy
- mixed tone in empty/error messages
- duplicated styling that should be centralized
