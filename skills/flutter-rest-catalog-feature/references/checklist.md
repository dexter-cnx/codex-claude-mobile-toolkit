# REST Catalog Checklist

- confirm the read-mostly API shape
- define list, detail, and related-resource routes
- decide which data should be cached locally
- define favorites and recently viewed storage if needed
- confirm explicit loading, empty, error, and stale states
- verify DTO mapping and repository behavior
- verify cache-first rendering
- avoid eager loading unless the UX requires it
