# Generator Pattern

1. read `assets/i18n/translations.csv`
2. infer locale columns from the header
3. convert dotted keys into nested JSON maps
4. write runtime artifacts to `assets/i18n/generated/`
