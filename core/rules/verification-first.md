# Verification first

No code task is complete without a verification summary.

Baseline:
- `flutter pub get`
- `dart format .`
- `flutter analyze`
- `flutter test`

Add UI and platform-specific validation where relevant.

If something cannot be verified, say so explicitly.
