# Localization Rules

- Externalize user-facing strings.
- Prefer stable keys with dotted paths.
- Use one CSV file as the source of truth when the project follows CSV-first localization.
- Ensure fallback behavior is defined.
