# API Integration Rules

- Use Dio or the project-standard HTTP client.
- Keep DTOs and mappers in data.
- Convert transport errors into app-friendly failures.
- Do not expose raw JSON or response maps to widgets.
