# Apply Instructions

1. Unzip this package at the root of `codex-claude-mobile-toolkit`.
2. Review the diff.
3. Confirm that no existing file with stronger project-specific content is being overwritten unintentionally.
4. Commit as one repository-structure upgrade or split into:
   - docs/rules update
   - pipeline prompts
   - skills
   - templates
   - Antigravity updates
5. Update any internal cross-links if the live repo has moved files since this package was generated.

Suggested commit title:

`feat(toolkit): add production workflow pipelines and repo-grade docs`
