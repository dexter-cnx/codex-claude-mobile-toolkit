# Task Template: Fix Task

Fix the reported issue with the smallest correct change.

## Requirements
- identify probable root cause
- avoid broad rewrites
- verify the affected flow directly if possible

## Required output
- root cause
- files changed
- verification result
- follow-up if the fix is partial
